name: wechat_clone_new
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.5.0

dependencies:
  flutter:
    sdk: flutter

  # State Management
  provider: ^6.1.1
  
  # HTTP Requests
  http: ^1.2.0
  dio: ^5.4.0
  
  # Local Storage
  shared_preferences: ^2.2.2
  
  # Image Handling
  cached_network_image: ^3.3.1
  image_picker: ^1.0.7
  
  # UI Components
  shimmer: ^3.0.0
  
  # Video
  video_player: ^2.8.2
  chewie: ^1.7.5
  
  # Camera
  camera: ^0.10.5+9
  
  # Location
  geolocator: ^11.0.0
  
  # File Handling
  path_provider: ^2.1.2
  path: ^1.9.0
  file_picker: ^6.1.1
  
  # Permissions
  permission_handler: ^11.2.0
  
  # Date/Time
  intl: ^0.19.0
  
  # UI
  carousel_slider: ^4.2.1
  
  # Firebase (Optional - comment out if not needed)
  # firebase_core: ^3.6.0
  # firebase_auth: ^5.3.1
  # cloud_firestore: ^5.4.4
  
  cupertino_icons: ^1.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
